import{useEffect,useState}from'react';import{api}from'../api';import{CheckCircle2,AlertTriangle,Activity}from'lucide-react';
export default function Model(){const[m,setM]=useState<any>();const[u,setU]=useState<any>();const[d,setD]=useState<any>();useEffect(()=>{api('/model').then(setM);api('/url/model').then(setU);api('/monitoring/drift').then(setD).catch(()=>{})},[]);if(!m)return <div className="loading">Loading models…</div>;const p=m.product_thresholds;return <div className="page"><header className="page-head"><div><h1>Models</h1><p>Model status, operating policy, validation evidence, and offline monitoring.</p></div></header><div className="model-grid"><section className="panel"><div className="model-title"><div><span className="status-pill active">Active</span><h2>Payment Fraud V3</h2><p>{m.version}</p></div><CheckCircle2/></div><div className="metric-row"><Metric l="ROC-AUC" v={`${(m.metrics?.roc_auc*100||91.1596).toFixed(2)}%`}/><Metric l="PR-AUC" v={`${(m.metrics?.pr_auc*100||56.2946).toFixed(2)}%`}/></div><h3>Operating policy</h3><div className="policy-list"><Policy n="01" t="Low" d="Below monitoring threshold"/><Policy n="02" t="Monitor" d={`From ${(p.monitor.threshold*100).toFixed(2)}%`}/><Policy n="03" t="Review" d={`From ${(p.review.threshold*100).toFixed(2)}% · creates alert`}/><Policy n="04" t="Automatic case" d={`From ${(p.automatic_case.threshold*100).toFixed(2)}% · creates investigation`}/></div></section><section className="panel"><div className="model-title"><div><span className="status-pill experimental">Experimental</span><h2>URL Risk V2</h2><p>{u?.version||'Lexical URL model'}</p></div><AlertTriangle/></div><div className="notice"><b>Automated enforcement disabled</b><p>The current artifact failed independent legitimate-domain stress tests. Its score is available for inspection but is not used for allow/block decisions.</p></div><h3>Validation status</h3><div className="policy-list"><Policy n="01" t="Dataset evaluation" d="PhiUSIIL held-out split"/><Policy n="02" t="Independent stress test" d="Failed acceptance gates"/><Policy n="03" t="Production decision" d="Rejected"/></div></section></div><section className="panel drift-panel"><div className="panel-head"><div><h2>Offline score drift</h2><p>Replay monitoring only — this project is not connected to a live production stream.</p></div><Activity/></div>{!d?<div className="muted">Loading monitoring status…</div>:d.status==='insufficient_data'?<div className="notice"><b>Insufficient assessed history</b><p>{d.assessed_rows} assessed rows available; at least {d.minimum_rows} are required for a meaningful comparison.</p></div>:<div className="drift-grid"><Metric l="PSI" v={d.psi.toFixed(3)}/><Metric l="Status" v={d.status.replaceAll('_',' ')}/><Metric l="Reference mean" v={`${d.reference_mean_score.toFixed(2)}%`}/><Metric l="Current mean" v={`${d.current_mean_score.toFixed(2)}%`}/></div>}<p className="scope-copy">Method: {d?.method||'Population Stability Index after enough assessments are available.'}</p></section></div>}
function Metric({l,v}:any){return <div className="metric"><span>{l}</span><strong>{v}</strong></div>}function Policy({n,t,d}:any){return <div className="policy"><span>{n}</span><div><b>{t}</b><p>{d}</p></div></div>}
